# File contains functions when cropping is enabled in main.py

def crop(img):
    """
    Crops an image to a given inset
    """


def crop_all(datapath, dataset):
    """
    Crops and exports every image of the defined imageset
    """
